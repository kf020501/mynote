## ドキュメントと回答

読み手(人・AI)の作業量を最小にする。文章量と情報量は比例しない。

- 削除テスト: 消しても意味が変わらない語・文・見出しは消す
- 削らない: 背景・結論・判断根拠・人名・日付・数値
- 改善・レビュー依頼では項目を足さない。追加案は本文でなく回答に書く
- 経緯・根拠が本文より長い場合のみ別ファイルに分けてリンクする
- 結論から書き、前置きと繰り返しを省く(回答も同じ)

## ネット調査結果の記載

ウェブ情報を根拠に書くときは次の形式にする。

```
<結論>

[<ページ名>](<URL>)
> <引用文>
```

- 引用文は本文を取得したページから原文のままコピーする（要約・和訳・語尾変更をしない）。取得できないページは引用しない
- 引用は結論1つにつき最も直接的な1〜2件
- 引用で裏付けていない記述は文末に「（推測）」とだけ付ける。裏付けた記述には何も付けない